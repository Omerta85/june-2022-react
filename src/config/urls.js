const baseUrls = {
    jsonPlaceHolder: 'https://jsonplaceholder.typicode.com',
}

const _urls = {
    todos: '/todos',
    albums: '/albums',
    comments: '/comments',
    posts: '/posts'
}

export {baseUrls, _urls}